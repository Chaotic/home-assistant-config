---
# The Tautulli sensor platform will monitor activity on a given Tautulli Server.
# It will create a sensor that shows the number of currently active streams as the state.
#
# https://www.home-assistant.io/components/sensor.tautulli/
#
platform: tautulli
api_key: !secret tautulli_api_key
host: !secret server_ip
monitored_users:
  - ChaoticUnreal
  - Kedar
  - Erin
monitored_conditions:
  - library_name
  - full_title